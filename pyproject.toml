[project]
name = "django-skeleton"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0",
    "djangorestframework>=3.15",
    "mysqlclient>=2.2",
]

[tool.uv]
dev-dependencies = [
    "factory-boy>=3.3",
]
